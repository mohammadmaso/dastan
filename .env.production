# Production deployment (shared host — avoids ports used by raheto-v3)
WEB_PORT=3002
API_PORT=3001
MEMORY_PORT=8000

# Browser fallback when not using the Next.js /api rewrite
NEXT_PUBLIC_API_URL=http://87.107.5.75:3001
